2026-03-20 Task 1 baseline: the trustworthy local reproduction came from stopping Docker and running `sudo antigravity_tools`, not from the pre-existing `docker compose` service on port 8045.
2026-03-20 Task 1 baseline: local run resolved data root to `/root/.antigravity_tools` and reproduced `onboardUser: done=true but no project_id in response` followed by `Onboarding completed for pemovet703@gmail.com: 14 models, tier: Unknown` for account `9502583d-8eaf-4464-bfa7-271dde305e56`.
2026-03-20 Task 1 verification gap: allowed admin HTTP endpoints (`/api/accounts`, `/api/accounts/current`, `/api/accounts/export`, `/api/accounts/:accountId/quota`) do not expose persisted `token.project_id`; that field is present only on internal/Tauri full-account serialization paths.
2026-03-20 Task 1 verification gap: a correctly routed Axum `Json<T>` handler should return a JSON body, so the missing onboard response body is stronger evidence of hitting the wrong service/path or losing the valid-run capture than of an intentional empty JSON success response.
2026-03-20 Source Analysis: Discovered mismatch between frontend request mapping (`src/utils/request.ts`) and backend route table (`src-tauri/src/proxy/server.rs`). The `/api/accounts/:accountId/onboard` route is missing in the current Axum implementation.
2026-03-20 Data Exposure: Persisted `project_id` is accessible via Tauri command surfaces (`list_accounts`, `get_current_account`) but is deliberately excluded from admin HTTP response shapes.
2026-03-20 Task 2 parity: the shared backend contract now lives in `src-tauri/src/modules/quota.rs` as `resolve_project_with_contract()` plus `ProjectResolutionOutcome`, and both `project_resolver.rs` and the touched runtime/warmup paths in `token_manager.rs` consume that explicit outcome model instead of manufacturing a default project.
2026-03-20 Task 2 parity: `get_valid_token_for_warmup()` was the remaining hidden fallback leak after the first refactor pass; aligning that helper required turning missing project resolution into an explicit warmup error instead of returning `bamboo-precept-lgxtn`.
2026-03-20 Task 4 verification: `cargo check` with `CARGO_TARGET_DIR=/tmp/antigravity-task4-target` proved the root-owned default `src-tauri/target` directory is a separate blocker from compilation; once isolated, the next hard blocker is missing system `pkg-config` and GTK/glib dev libraries rather than the edited Rust code itself.
2026-03-20 Task 5 parity: the remaining runtime divergence was the public `modules::quota::fetch_quota()` entrypoint still forcing fresh resolution instead of reusing a persisted real `token.project_id`; fixing that one shared caller aligned account refresh/test/runtime follow-up behavior with the Task 4 retention rule.
